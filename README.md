# Telegram Bridge

```shell
# clone this repo
git clone https://github.com/LS-KR/TelegramBridge.git

# install dependencies
pip3 install -r requirements.txt

# copy config
mv config.example.json config.json

# edit config
vim config.json

# start server
python3 main.py
```